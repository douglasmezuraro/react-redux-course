const INITIAL_STATE = {
    summary: {
        credit: 0.00,
        debt: 0.00,
    },
};

const dashBoardReducer = (state = INITIAL_STATE) => {
    return state;
};

export default dashBoardReducer;
